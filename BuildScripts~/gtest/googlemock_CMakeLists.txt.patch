@@ -100,6 +100,9 @@ if (DEFINED CMAKE_VERSION AND NOT "${CMA
   target_include_directories(gmock_main INTERFACE "${gmock_SOURCE_DIR}/include")
 endif()
 
+target_include_directories(gmock      PRIVATE "${LIBCXX_INCLUDE_DIRS}")
+target_include_directories(gmock_main PRIVATE "${LIBCXX_INCLUDE_DIRS}")
+
 ########################################################################
 #
 # Install rules
