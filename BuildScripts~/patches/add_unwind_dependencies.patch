--- BUILD.gn	2023-04-07 07:23:28.709083582 +0900
+++ BUILD.gn.patch	2023-04-18 22:56:54.059641084 +0900
@@ -91,6 +91,11 @@ config("runtime_library") {
     libs += [ "android_support" ]
   }
 
+  # arm builds of libc++ starting in NDK r12 depend on unwind.
+  if (current_cpu == "arm") {
+    libs += [ "unwind" ]
+  }
+
   if (current_cpu == "arm" && arm_version == 6) {
     libs += [ "atomic" ]
   }
